# Terroir Ops — local-mode requirements (Windows + Mac + Linux)

# Data handling
pandas>=2.0.0
openpyxl>=3.1.0

# Web API
fastapi>=0.100.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6   # required for file uploads (OCS order template)

# Competitor scraping (HiBuddy pilot)
requests>=2.31.0
beautifulsoup4>=4.12.0
lxml>=5.0.0

# Note: SQLite is built into Python, no install needed.
